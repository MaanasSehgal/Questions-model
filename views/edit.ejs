<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Update a question</title>
        <style>
            body {
                font-family: "Arial", sans-serif;
                background-color: #f4f4f4;
                margin: 20px;
            }

            form {
                background-color: #fff;
                padding: 20px;
                border-radius: 8px;
                box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
                max-width: 400px;
                margin: 0 auto;
            }

            label {
                display: block;
                margin-bottom: 8px;
            }

            input {
                padding: 8px;
                margin-bottom: 12px;
                box-sizing: border-box;
                border: 1px solid #ccc;
                border-radius: 4px;
            }

            .type-container {
                display: flex;
                align-items: center;
                margin-bottom: 12px;
            }

            button {
                border-radius: 15px;
            }

            button {
                background-color: #4caf50;
                color: #fff;
                padding: 10px 15px;
                border: none;
                border-radius: 4px;
                cursor: pointer;
            }

            button:hover {
                background-color: #2980b9;
            }
        </style>
    </head>
    <body>
        <form method="POST" action="/questions/edit/<%= question._id %>?_method=PATCH">
            <label for="question">Question:</label>
            <input name="question" id="question" type="text" placeholder="Enter the question" value="<%= question.question %>" required />

            <label for="answer">Answer:</label>
            <input name="answer" id="answer" type="text" placeholder="Enter the answer" value="<%= question.answer %>" required />

            <div class="type-container">
                <label>Type:</label>
                <input type="radio" name="type" value="main" id="main" <% if (question.type === 'main') { %>checked<% } %> required />
                <label for="main">Main</label>

                <input type="radio" name="type" value="side" id="side" <% if (question.type === 'side') { %>checked<% } %> required />
                <label for="side">Side</label>
            </div>

            <button type="submit">Edit</button>
        </form>
    </body>
</html>
